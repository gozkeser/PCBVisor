# PCBVisor Project Overview

PCBVisor is a command-line tool set for detecting circular fiducial markers in PNG images. It uses contour analysis with circularity and radius filtering to find circles, deduplicates close detections, and exports annotated images and JSON metadata.
